# Fix MapData.Id Property Error

## Error Analysis
- **Error**: `'MapData' does not contain a definition for 'Id'`
- **Location**: `XMLDataLoader.cs` line 260
- **Root Cause**: The code is trying to access `mapData.Id` but the MapData class has a property called `MapId` instead

## Fix Plan

### 1. Fix XMLDataLoader.cs
- **Action**: Replace `mapData.Id` with `mapData.MapId`
- **File**: `Assets\Scripts\DataLoaders\XMLDataLoader.cs`
- **Change**: Update line 260 to use the correct property name

### 2. Verify Fix
- **Action**: Run diagnostics to confirm the error is resolved
- **Files**: All files

## Expected Result
After the fix, the compilation error will be resolved, and the code will correctly access the MapId property of the MapData class.